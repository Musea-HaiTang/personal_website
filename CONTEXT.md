# 个人网站领域词汇

单用户个人网站，聚合首页 / 计划 / 日记 / 番茄钟 / 导航 / 学习笔记等模块共享一套界面语言；本词汇表收录跨模块与笔记模块的规范术语。

## 学习笔记（Notes）

**笔记（Note）**:
一条只读的学习资料记录，包含标题、分类、标签、正文与更新时间；正文以 Markdown 文件落盘，不提供编辑。
_Avoid_: 文章、文档

**分类（Folder）**:
笔记的组织维度；一条笔记属于且仅属于一个分类，列表页与阅读弹窗按分类过滤和切换。
_Avoid_: 文件夹、目录

**阅读弹窗（Reader modal）**:
以模态方式打开单条笔记并只读展示正文的界面；不提供编辑与删除入口。
_Avoid_: 编辑器、详情页

**大纲（Outline）**:
阅读弹窗侧栏中由正文标题（h1/h2/h3）生成的导航树；有子级的标题可折叠，点击条目平滑跳转到正文对应位置。
_Avoid_: 目录树、TOC

**侧栏开关（Sidebar toggle）**:
展开 / 收起大纲侧栏的圆形按钮；收起后进入沉浸阅读，弹窗窗口尺寸不变。
_Avoid_: 菜单按钮、折叠按钮

**沉浸阅读（Immersive reading）**:
收起侧栏后正文加宽占满弹窗的阅读状态；与「全屏」不同，弹窗尺寸不变化。
_Avoid_: 全屏模式

**文件页签（File tab）**:
阅读弹窗侧栏中与「大纲」并列的页签，按当前笔记的分类列出同分类笔记并直接切换当前阅读笔记。
_Avoid_: 文件树、笔记切换

## 计划与周（Tasks）

**周（Week）**:
周一至周日；「一周」= 当周计划 + 计划下的子任务 + 当周独立任务（未挂 `subtask_id` 的当日任务）。子任务关联的当日任务视为该子任务的执行实例，由子任务代表，不重复计数。
_Avoid_: 日程、流水

**本周计划（Weekly plan）**:
一周的大方向，含多个子任务；子任务全部完成即计划完成。以 `week_start`（周一）唯一标识一周。
_Avoid_: 周目标、周任务

**子任务（Subtask）**:
属于某个本周计划的小事项，带名字、备注、重要度与完成状态；完成率统计对象之一。
_Avoid_: 待办、清单项

**独立任务（Standalone task）**:
安排到某一天的当日任务，但未关联任一子任务（`subtask_id` 为空）；与子任务一起构成一周完成率的统计对象。
_Avoid_: 每日任务、自由任务

**周聚合（Week aggregate）**:
一次取齐一周的计划、计划子任务与独立任务，并派生完成数/总数/完成率/每日完成计数的模块（`backend/app/services/week.py`）；周统计、周总结、周导出共用，避免各自重复取数与口径不一。
_Avoid_: 周报表、周服务

**完成率（Completion rate）**:
`(子任务完成 + 独立任务完成) / (子任务 + 独立任务)`，周统计与周导出统一使用该口径；无统计对象时为 0。
_Avoid_: 完成度、进度
