# 数据目录（SQLite 数据库与 Markdown 日记正文），相对于 backend 目录
DATA_DIR=./data
# 统一时区
TIMEZONE=Asia/Shanghai
# 公网登录开关，P0 保持 false
AUTH_ENABLED=false
# P1 向量索引：不配置时索引会重试后标记失败，不影响笔记浏览
ZHIPU_API_KEY=
EMBEDDING_MODEL=embedding-3
